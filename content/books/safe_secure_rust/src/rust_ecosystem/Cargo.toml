[package]
name = "car_project"
version = "0.0.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
cargo-audit = "0.20.0"
blurhash = "0.1.1" # Vulnerable library, cargo will complain
